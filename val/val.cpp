#include <SFML/Graphics.hpp>
/*
int main()
{
    float i = 10, j = 10;

    sf::RenderWindow window(sf::VideoMode(200, 200), "SFML works!");
    sf::RectangleShape shape(const Vector2f &size = Vector2f(10, 10));


    while (window.isOpen())
    {
        sf::Event event;
        while (window.pollEvent(event))
        {
            if (event.type == sf::Event::Closed)
                window.close();
        }

        window.clear();
        window.draw(shape);
        window.display();
        shape.setPosition(sf::Vector2f(20 * i, 20 * j));
        
    }

    return 0;
}*/